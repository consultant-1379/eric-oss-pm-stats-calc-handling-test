{
    "thresholds": {},
    "scenarios": {
        "default": {
            "executor": "per-vu-iterations",
            "maxDuration": "150m",
            "iterations": 1
        },
        "parallelQuerySet": {
            "executor": "per-vu-iterations",
            "exec": "parallelQuerySet",
            "maxDuration": "150m",
            "iterations": 1,
            "vus": 10
        }
    },
    "summaryTrendStats": ["min", "avg", "max", "med", "p(90)", "p(95)", "p(99)", "count"],
    "insecureSkipTLSVerify": true
}
